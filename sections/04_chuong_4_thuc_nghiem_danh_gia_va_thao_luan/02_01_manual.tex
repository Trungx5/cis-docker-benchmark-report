\subsubsection{Manual}\label{trien-khai-audit-manual}

Kiểm toán thủ công được thực hiện trực tiếp trên terminal của host để đối chứng và kiểm tra các cấu hình mà hệ thống tự động không thể quét hoặc yêu cầu đánh giá của quản trị viên.

\textbf{Bảng chi tiết các hoạt động Audit thủ công:}

{\def\LTcaptype{none} % do not increment counter
\begin{longtable}[]{@{}|
  >{\raggedright\arraybackslash}p{(\linewidth - 8\tabcolsep - 5\arrayrulewidth) * \real{0.0739}}|
  >{\raggedright\arraybackslash}p{(\linewidth - 8\tabcolsep - 5\arrayrulewidth) * \real{0.2995}}|
  >{\raggedright\arraybackslash}p{(\linewidth - 8\tabcolsep - 5\arrayrulewidth) * \real{0.3000}}|
  >{\raggedright\arraybackslash}p{(\linewidth - 8\tabcolsep - 5\arrayrulewidth) * \real{0.3266}}|@{}}
\hline
\begin{minipage}[b]{\linewidth}\raggedright
\textbf{Phần}
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
\textbf{Benchmark}
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
\textbf{Phương pháp thực hiện}
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
\textbf{Hành động thực hiện}
\end{minipage} \\
\hline
\endhead

\multirow{19}{=}{1} & Phân vùng riêng cho các container & \begin{minipage}[t]{\linewidth}\raggedright
grep \textquotesingle/var/lib/docker\textbackslash s\textquotesingle{} /proc/mounts\\
mountpoint -\/- "\$(docker info -f \textquotesingle\{\{ .DockerRootDir \}\}\textquotesingle)"\strut
\end{minipage} & Chạy lệnh \texttt{mountpoint} để kiểm tra phân vùng riêng biệt của Docker. \\
\cline{2-4}
& Chỉ cho trusted user kiểm soát Docker Daemon & getent group docker & Kiểm tra các thành viên trong nhóm \texttt{docker} qua tệp \texttt{/etc/group}. \\
\cline{2-4}
& Cấu hình kiểm tra nhật ký cho Docker Daemon & auditctl -l \textbar{} grep /usr/bin/dockerd & Xác nhận sự hiện diện của quy tắc giám sát cho daemon dockerd. \\
\cline{2-4}
& Audit folder /run/containerd & auditctl -l \textbar{} grep /run/containerd & Xác nhận sự hiện diện của quy tắc giám sát cho thư mục /run/containerd. \\
\cline{2-4}
& Audit folder /var/lib/docker & auditctl -l \textbar{} grep /var/lib/docker & Xác nhận sự hiện diện của quy tắc giám sát cho thư mục /var/lib/docker. \\
\cline{2-4}
& Audit folder /etc/Docker & auditctl -l \textbar{} grep /etc/docker & Xác nhận sự hiện diện của quy tắc giám sát cho thư mục /etc/docker. \\
\cline{2-4}
& Audit docker.service & \begin{minipage}[t]{\linewidth}\raggedright
systemctl show -p FragmentPath docker.service\\
auditctl -l \textbar{} grep docker.service\strut
\end{minipage} & Kiểm tra sự hiện diện của quy tắc giám sát cho tệp \texttt{docker.service}. \\
\cline{2-4}
& Audt containerd.sock & \begin{minipage}[t]{\linewidth}\raggedright
grep \textquotesingle containerd.sock\textquotesingle{} /etc/containerd/config.toml\\
auditctl -l \textbar{} grep containerd.sock\strut
\end{minipage} & Kiểm tra sự hiện diện của quy tắc giám sát cho socket \texttt{containerd.sock}. \\
\cline{2-4}
& Audit docker.sock & \begin{minipage}[t]{\linewidth}\raggedright
systemctl show -p FragmentPath docker.sock\\
auditctl -l \textbar{} grep docker.sock\strut
\end{minipage} & Kiểm tra sự hiện diện của quy tắc giám sát cho socket \texttt{docker.sock}. \\
\cline{2-4}
& Audt /etc/default/docker & auditctl -l \textbar{} grep /etc/default/docker & Xác nhận sự hiện diện của quy tắc giám sát cho tệp /etc/default/docker. \\
\cline{2-4}
& Audit /etc/docker/daemon.json & auditctl -l \textbar{} grep /etc/docker/daemon.json & Xác nhận sự hiện diện của quy tắc giám sát cho tệp daemon.json. \\
\cline{2-4}
& Audit /etc/containerd/config.toml & auditctl -l \textbar{} grep /etc/containerd/config.toml & Xác nhận sự hiện diện của quy tắc giám sát cho tệp config.toml. \\
\cline{2-4}
& Aduti /etc/sysconfig/docker & auditctl -l \textbar{} grep /etc/sysconfig/docker & Xác nhận sự hiện diện của quy tắc giám sát cho tệp sysconfig/docker. \\
\cline{2-4}
& Audit /usr/bin/containerd-shim & auditctl -l \textbar{} grep /usr/bin/containerd-shim & Xác nhận sự hiện diện của quy tắc giám sát cho binary containerd-shim. \\
\cline{2-4}
& Audit /usr/bin/containerd-shim-runc-v1 & auditctl -l \textbar{} grep /usr/bin/containerd-shim-runc-v1 & Xác nhận sự hiện diện của quy tắc giám sát cho containerd-shim-runc-v1. \\
\cline{2-4}
& Audit /usr/bin/containerd-shim-runc-v2 & auditctl -l \textbar{} grep /usr/bin/containerd-shim-runc-v2 & Xác nhận sự hiện diện của quy tắc giám sát cho containerd-shim-runc-v2. \\
\cline{2-4}
& Audit /usr/bin/runc & auditctl -l \textbar{} grep /usr/bin/runc & Xác nhận sự hiện diện của quy tắc giám sát cho container runtime runc. \\
\cline{2-4}
& Đảm báo bảo mật cho container & Kiểm tra thủ công theo security benchmark của hệ thống (CIS, STIG...). Hỏi system admin về tiêu chuẩn áp dụng và xác minh các biện pháp đang thực thi. & Thực hiện kiểm tra các cấu hình hardening trên hệ điều hành host. \\
\cline{2-4}
& Đảm bảo phiên bản mới cho Docker kt & docker version & Đối chiếu phiên bản hiện tại của Docker với bản phát hành mới nhất. \\
\hline
\multirow{19}{=}{2} & Chạy docker daemon tránh quyền root & ps -fe \textbar{} grep \textquotesingle dockerd\textquotesingle{} & \\
\cline{2-4}
& Network trafic được đặt hạn chế giữa các container ở default bridge & docker network ls -\/-quiet \textbar{} xargs docker network inspect -\/-format \textquotesingle\{\{ .Name \}\}: \{\{ .Options \}\}\textquotesingle{} & \\
\cline{2-4}
& Logging level set ở ``info'' & grep "log-level" /etc/docker/daemon.json & \\
\cline{2-4}
& Docker được quyền thay đổi iptables & grep "-\/-iptables" /etc/docker/daemon.json & \\
\cline{2-4}
& Đảm bảo không sử dụng registry không an toàn & docker info -\/-format \textquotesingle Insecure Registries:\textquotesingle{} & \\
\cline{2-4}
& Đảm bảo không sử dụng trình điều khiển aufs & docker info -\/-format \textquotesingle Storage Driver: \{\{ .Driver \}\}\textquotesingle{} & \\
\cline{2-4}
& Đảm bảo không dùng devicemapper & docker info -\/-format \textquotesingle Storage Driver: \{\{ .Driver \}\}\textquotesingle{} & \\
\cline{2-4}
& Đảm bảo xác thực TLS cho Docker Daemon & grep "-\/-tls" /etc/docker/daemon.json & \\
\cline{2-4}
& Đảm bảo giới hạn ulimit mặc định & \begin{minipage}[t]{\linewidth}\raggedright
ps -ef \textbar{} grep dockerd\\
grep "default-ulimit" /etc/docker/daemon.json\strut
\end{minipage} & \\
\cline{2-4}
& Hỗ trợ user namespace & \begin{minipage}[t]{\linewidth}\raggedright
docker info \textbar{} grep -i userns\\
docker info -\/-format \textquotesingle\{\{ .SecurityOptions \}\}\textquotesingle{}\strut
\end{minipage} & \\
\cline{2-4}
& Cgroup sử dụng được xác nhận & grep "-\/-cgroup-parent" /etc/docker/daemon.json & \\
\cline{2-4}
& Đảm bảo device size không thay đổi trừ khi cần thiết & grep "-\/-storage-opt dm.basesize" /etc/docker/daemon.json & \\
\cline{2-4}
& Đảm bảo authorization cho Docker client & \begin{minipage}[t]{\linewidth}\raggedright
ps -ef \textbar{} grep dockerd\\
grep "authorization-plugins" /etc/docker/daemon.json\strut
\end{minipage} & \\
\cline{2-4}
& Đảm bảo container bj hạn chế không cho quyền mới & \begin{minipage}[t]{\linewidth}\raggedright
ps -ef \textbar{} grep dockerd\\
grep "no-new-privileges" /etc/docker/daemon.json\strut
\end{minipage} & \\
\cline{2-4}
& Đảm bảo container có live restore & \begin{minipage}[t]{\linewidth}\raggedright
docker info -\/-format \textquotesingle\{\{ .LiveRestoreEnabled \}\}\textquotesingle{}\\
grep "live-restore" /etc/docker/daemon.json\strut
\end{minipage} & \\
\cline{2-4}
& Đảm bảo có log cho đăng nhập gần và từ xa & \begin{minipage}[t]{\linewidth}\raggedright
docker info -\/-format \textquotesingle\{\{ .LoggingDriver \}\}\textquotesingle{}\\
grep "-\/-log-driver" /etc/docker/daemon.json\strut
\end{minipage} & \\
\cline{2-4}
& Đảm bảo Userland Proxy đã tắt & \begin{minipage}[t]{\linewidth}\raggedright
ps -ef \textbar{} grep dockerd\\
grep "userland-proxy" /etc/docker/daemon.json\strut
\end{minipage} & \\
\cline{2-4}
& Đảm bảo daemon-wide custom seccomp profile được thiết đặt & docker info -\/-format \textquotesingle\{\{ .SecurityOptions \}\}\textquotesingle{} & \\
\cline{2-4}
& Đảm bảo các tính năng thử nghiệm không được triển khai trong môi trường sản xuất & docker version -\/-format \textquotesingle\{\{ .Server.Experimental \}\}\textquotesingle{} & \\
\hline
\multirow{14}{=}{3} & Đảm bảo quyền root:root cho docker.service & \begin{minipage}[t]{\linewidth}\raggedright
systemctl show -p FragmentPath docker.service\\
stat -c \%U:\%G \textless FragmentPath\textgreater{} \textbar{} grep -v root:root\strut
\end{minipage} & \\
\cline{2-4}
& Đảm bảo docker.service cho quyền hợp lý & \begin{minipage}[t]{\linewidth}\raggedright
systemctl show -p FragmentPath docker.service\\
stat -c \%a \textless FragmentPath\textgreater{}\strut
\end{minipage} & \\
\cline{2-4}
& Đảm bảo quyền sở hữu của docker.socket được đặt thành root:root & \begin{minipage}[t]{\linewidth}\raggedright
systemctl show -p FragmentPath docker.socket\\
stat -c \%U:\%G \textless FragmentPath\textgreater{} \textbar{} grep -v root:root\strut
\end{minipage} & \\
\cline{2-4}
& Đảm bảo quyền truy cập tệp docker.socket được đặt thành 644 hoặc hạn chế & \begin{minipage}[t]{\linewidth}\raggedright
systemctl show -p FragmentPath docker.socket\\
stat -c \%a \textless FragmentPath\textgreater{}\strut
\end{minipage} & \\
\cline{2-4}
& Đảm bảo quyền sỡ hữu thư mục /etc/docker được đặt thành root:root & stat -c \%U:\%G /etc/docker \textbar{} grep -v root:root & \\
\cline{2-4}
& Đảm bảo quyền truy cập thư mục /etc/docker được đặt thành 755 hoặc hạn chế & stat -c \%a /etc/docker & \\
\cline{2-4}
& Đảm bảo quyền truy cập tệ docker.socker được đặt thành 644 hoặc hạn chế & \begin{minipage}[t]{\linewidth}\raggedright
stat -c \%a /var/run/docker.sock\\
stat -c \%a /usr/lib/systemd/\\
system/docker.socket\strut
\end{minipage} & \\
\cline{2-4}
& Đảm bảo quyền root:root cho sỡ hữu tệp chứng chỉ & stat -c \%U:\%G /etc/docker/certs.d/* \textbar{} grep -v root:root & \\
\cline{2-4}
& Đảm bảo quyền truy cập chứng trí registry được đặt thành 444 hoặc hạn chế hơn & find /etc/docker/certs.d/ -type f -exec stat -c "\%a \%n" \{\} \textbackslash; & \\
\cline{2-4}
& Đảm bảo quyền sỡ hữu Docker server là root:root và file permission 4444 & \begin{minipage}[t]{\linewidth}\raggedright
stat -c \%U:\%G \textless path\_to\_tlscert\textgreater{} \textbar{} grep -v root:root\\
stat -c \%a \textless path\_to\_tlscert\textgreater{}\strut
\end{minipage} & \\
\cline{2-4}
& Đảm bảo quyền Docker socker file ownership là root:docker và để file permission 660 & \begin{minipage}[t]{\linewidth}\raggedright
stat -c \%U:\%G /var/run/docker.sock \textbar{} grep -v root:docker\\
stat -c \%a /var/run/docker.sock\strut
\end{minipage} & \\
\cline{2-4}
& Đảm bảo quyền daemon.json là root:root và file permssion 644 & \begin{minipage}[t]{\linewidth}\raggedright
stat -c \%U:\%G /etc/docker/daemon.json \textbar{} grep -v root:root\\
stat -c \%a /etc/docker/daemon.json\strut
\end{minipage} & \\
\cline{2-4}
& Đảm bảo quyền /etc/sysconfig/docker là root:root và file permission 644 & \begin{minipage}[t]{\linewidth}\raggedright
stat -c \%U:\%G /etc/sysconfig/docker \textbar{} grep -v root:root\\
stat -c \%a /etc/sysconfig/docker\strut
\end{minipage} & \\
\cline{2-4}
& Đảm bảo Containered socket có quyền là root:root và file permission 660 & \begin{minipage}[t]{\linewidth}\raggedright
stat -c \%U:\%G /run/containerd/containerd.sock \textbar{} grep -v root:root\\
stat -c \%a /run/containerd/containerd.sock\strut
\end{minipage} & \\
\hline
\multirow{9}{=}{4} & Phải có container tạo, chạy trong trusted base images và không có package dư thừa không cần thiết được tải về. & \begin{minipage}[t]{\linewidth}\raggedright
docker images\\
docker history \textless imageName\textgreater{}\\
docker exec \$INSTANCE\_ID rpm -qa \# hoặc dpkg -l\strut
\end{minipage} & \\
\cline{2-4}
& Đảm bảo image được scan và phải có security patches & \begin{minipage}[t]{\linewidth}\raggedright
docker ps -\/-quiet\\
\# Dùng tool scan (Trivy, Grype, Clair...):\\
trivy image \textless IMAGE\_NAME\textgreater{}\strut
\end{minipage} & \\
\cline{2-4}
& Phải có HEALTHCHECK instruction & docker inspect -\/-format=\textquotesingle\{\{ .Config.Healthcheck \}\}\textquotesingle{} \textless IMAGE\textgreater{} & \\
\cline{2-4}
& Phải có update instruction và không sử dụng một mình trong Dockerfiles & \begin{minipage}[t]{\linewidth}\raggedright
docker images\\
docker history \textless Image\_ID\textgreater{}\strut
\end{minipage} & \\
\cline{2-4}
& Không cung cấp quyền Setuid và Getuid & docker export \textless IMAGE\_ID\textgreater{} \textbar{} tar -tv 2\textgreater/dev/null \textbar{} grep -E \textquotesingle\^{}{[}-rwx{]}.*(s\textbar S).*\textbackslash s{[}0-9{]}\textquotesingle{} & \\
\cline{2-4}
& Dùng COPY thay vì ADD & \begin{minipage}[t]{\linewidth}\raggedright
docker images\\
docker history \textless IMAGE\_ID\textgreater{}\strut
\end{minipage} & \\
\cline{2-4}
& Secrets không được bỏ vào Dockerfiles & \begin{minipage}[t]{\linewidth}\raggedright
docker images\\
docker history \textless IMAGE\_ID\textgreater{}\strut
\end{minipage} & \\
\cline{2-4}
& Các package phải chính thống khi tải về & \begin{minipage}[t]{\linewidth}\raggedright
docker images\\
docker history \textless IMAGE\_ID\textgreater{}\strut
\end{minipage} & \\
\cline{2-4}
& Các signed artifacts phải được kiểm chứng & \begin{minipage}[t]{\linewidth}\raggedright
\# Xác minh chữ ký của artifact trước khi upload:\\
cosign verify \textless IMAGE\textgreater{}\\
\# Hoặc kiểm tra chữ ký GPG cho package\strut
\end{minipage} & \\
\hline
\multirow{28}{=}{5} & Không bật swarm mode nếu không cần thiết & docker info -\/-format \textquotesingle\{\{ .Swarm \}\}\textquotesingle{} & \\
\cline{2-4}
& Bật AppArmor Profile & docker ps -\/-quiet -\/-all \textbar{} xargs docker inspect -\/-format \textquotesingle\{\{ .Id \}\}: AppArmorProfile=\{\{ .AppArmorProfile \}\}\textquotesingle{} & \\
\cline{2-4}
& Phải có SELinux & docker ps -\/-quiet -\/-all \textbar{} xargs docker inspect -\/-format \textquotesingle\{\{ .Id \}\}: SecurityOpt=\{\{ .HostConfig.SecurityOpt \}\} MountLabel=\{\{ .MountLabel \}\} ProcessLabel=\{\{ .ProcessLabel \}\}\textquotesingle{} & \\
\cline{2-4}
& Linux Kernel phải để chế độ hạn chế trong containers & docker ps -\/-quiet -\/-all \textbar{} xargs docker inspect -\/-format \textquotesingle\{\{ .Id \}\}: CapAdd=\{\{ .HostConfig.CapAdd \}\} CapDrop=\{\{ .HostConfig.CapDrop \}\}\textquotesingle{} & \\
\cline{2-4}
& Không sử dụng privileged container & docker ps -\/-quiet -\/-all \textbar{} xargs docker inspect -\/-format \textquotesingle\{\{ .Id \}\}: Privileged=\{\{ .HostConfig.Privileged \}\}\textquotesingle{} & \\
\cline{2-4}
& Không mount các host system cần bảo vệ chặt trong container & docker ps -\/-quiet -\/-all \textbar{} xargs docker inspect -\/-format \textquotesingle\{\{ .Id \}\}: Volumes=\{\{ .Mounts \}\}\textquotesingle{} & \\
\cline{2-4}
& SSHD không chạy trong containers & docker ps -\/-quiet \textbar{} xargs -I\{\} docker exec \{\} ps -el & \\
\cline{2-4}
& Không chạy port đặc quyền trong container & docker ps -\/-quiet \textbar{} xargs docker inspect -\/-format \textquotesingle\{\{ .Id \}\}: Ports=\{\{ .NetworkSettings.Ports \}\}\textquotesingle{} & \\
\cline{2-4}
& Chỉ chạy port cần thiết cho container & docker ps -\/-quiet \textbar{} xargs docker inspect -\/-format \textquotesingle\{\{ .Id \}\}: Ports=\{\{ .NetworkSettings.Ports \}\}\textquotesingle{} & \\
\cline{2-4}
& Bảo mật network namespace & docker ps -\/-quiet -\/-all \textbar{} xargs docker inspect -\/-format \textquotesingle\{\{ .Id \}\}: NetworkMode=\{\{ .HostConfig.NetworkMode \}\}\textquotesingle{} & \\
\cline{2-4}
& Sử dụng tài nguyên hợp lý: Memory usage và CPU priority(5.11, 5.12) & \begin{minipage}[t]{\linewidth}\raggedright
docker ps -\/-quiet -\/-all \textbar{} xargs docker inspect -\/-format \textquotesingle\{\{ .Id \}\}: Memory=\{\{ .HostConfig.Memory \}\}\textquotesingle{}\\
docker ps -\/-quiet -\/-all \textbar{} xargs docker inspect -\/-format \textquotesingle\{\{ .Id \}\}: CpuShares=\{\{ .HostConfig.CpuShares \}\}\textquotesingle{}\strut
\end{minipage} & \\
\cline{2-4}
& Để filesystem của container's ở quyền ``read only'' & docker ps -\/-quiet -\/-all \textbar{} xargs docker inspect -\/-format \textquotesingle\{\{ .Id \}\}: ReadonlyRootfs=\{\{ .HostConfig.ReadonlyRootfs \}\}\textquotesingle{} & \\
\cline{2-4}
& Mạng lưu thông phải đến một interface được đặt & docker ps -\/-quiet \textbar{} xargs docker inspect -\/-format \textquotesingle\{\{ .Id \}\}: Ports=\{\{ .NetworkSettings.Ports \}\}\textquotesingle{} & \\
\cline{2-4}
& Có chính sách ``on-failure'' thì khởi động lại với giá trị 5 & \begin{minipage}[t]{\linewidth}\raggedright
docker ps -\/-quiet -\/-all \textbar{} xargs docker inspect -\/-format \textquotesingle\{\{ .Id \}\}: RestartPolicyName=\{\{ .HostConfig.RestartPolicy.Name \}\}\\
MaximumRetryCount=\{\{ .HostConfig.RestartPolicy.\\
MaximumRetryCount \}\}\textquotesingle{}\strut
\end{minipage} & \\
\cline{2-4}
& Bảo mật Host's process namespace và IPC namespace(5.16, 5.17) & \begin{minipage}[t]{\linewidth}\raggedright
docker ps -\/-quiet -\/-all \textbar{} xargs docker inspect -\/-format \textquotesingle\{\{ .Id \}\}: PidMode=\{\{ .HostConfig.PidMode \}\}\textquotesingle{}\\
docker ps -\/-quiet -\/-all \textbar{} xargs docker inspect -\/-format \textquotesingle\{\{ .Id \}\}: IpcMode=\{\{ .HostConfig.IpcMode \}\}\textquotesingle{}\strut
\end{minipage} & \\
\cline{2-4}
& Host Devices không nên tương tác chung với containers & docker ps -\/-quiet -\/-all \textbar{} xargs docker inspect -\/-format \textquotesingle\{\{ .Id \}\}: Devices=\{\{ .HostConfig.Devices \}\}\textquotesingle{} & \\
\cline{2-4}
& Phải có default limit overwritten trong lúc chạy nếu cần thiết & docker ps -\/-quiet -\/-all \textbar{} xargs docker inspect -\/-format \textquotesingle\{\{ .Id \}\}: Ulimits=\{\{ .HostConfig.Ulimits \}\}\textquotesingle{} & \\
\cline{2-4}
& Để mount propagation và host's UTS namespace bí mật(5.20,5.21) & \begin{minipage}[t]{\linewidth}\raggedright
docker ps -\/-quiet -\/-all \textbar{} xargs docker inspect -\/-format \textquotesingle\{\{ .Id \}\}: Propagation=\{\{range \$mnt := .Mounts\}\} \{\{json \$mnt.Propagation\}\} \{\{end\}\}\textquotesingle{}\\
docker ps -\/-quiet -\/-all \textbar{} xargs docker inspect -\/-format \textquotesingle\{\{ .Id \}\}: UTSMode=\{\{ .HostConfig.UTSMode \}\}\textquotesingle{}\strut
\end{minipage} & \\
\cline{2-4}
& Chế độ của default seccomp profile không tắt & docker ps -\/-quiet -\/-all \textbar{} xargs docker inspect -\/-format \textquotesingle\{\{ .HostConfig.SecurityOpt \}\}\textquotesingle{} & \\
\cline{2-4}
& Khi sử dụng docker exec commands, không sử dụng quyền priviledged option hoặc user=root(5.23,5.24) & \begin{minipage}[t]{\linewidth}\raggedright
ausearch -k docker \textbar{} grep exec \textbar{} grep privileged\\
ausearch -k docker \textbar{} grep exec \textbar{} grep user\strut
\end{minipage} & \\
\cline{2-4}
& Phải có cgroup usage & docker ps -\/-quiet -\/-all \textbar{} xargs docker inspect -\/-format \textquotesingle\{\{ .Id \}\}: CgroupParent=\{\{ .HostConfig.CgroupParent \}\}\textquotesingle{} & \\
\cline{2-4}
& Container chỉ cho quyền cần thiết và không thêm quyền không cần thiết & docker ps -\/-quiet -\/-all \textbar{} xargs docker inspect -\/-format \textquotesingle\{\{ .HostConfig.SecurityOpt \}\}\textquotesingle{} & \\
\cline{2-4}
& Container health luôn chạy & docker ps -\/-quiet \textbar{} xargs docker inspect -\/-format \textquotesingle\{\{ .Id \}\}: Health=\{\{ .State.Health.Status \}\}\textquotesingle{} & \\
\cline{2-4}
& Docker command phải sử dụng lệnh mới nhất của image & \begin{minipage}[t]{\linewidth}\raggedright
\# Kiểm tra image repository\\
docker pull \textless IMAGE\textgreater{} \# Quan sát status\\
\# So sánh digest của image đang chạy với image mới nhất\strut
\end{minipage} & \\
\cline{2-4}
& PIDs cgroup limit phải được sử dụng & docker ps -\/-quiet -\/-all \textbar{} xargs docker inspect -\/-format \textquotesingle\{\{ .Id \}\}: PidsLimit=\{\{ .HostConfig.PidsLimit \}\}\textquotesingle{} & \\
\cline{2-4}
& Không sử dụng default bridge `docker0' & docker network ls -\/-quiet \textbar{} xargs docker network inspect -\/-format \textquotesingle\{\{ .Name \}\}: \{\{ .Options \}\}\textquotesingle{} & \\
\cline{2-4}
& Không chia sẽ host's user namespace & docker ps -\/-quiet -\/-all \textbar{} xargs docker inspect -\/-format \textquotesingle\{\{ .Id \}\}: UsernsMode=\{\{ .HostConfig.UsernsMode \}\}\textquotesingle{} & \\
\cline{2-4}
& Docker socker không được mount vào bất kỳ container nào & docker ps -\/-quiet -\/-all \textbar{} xargs docker inspect -\/-format \textquotesingle\{\{ .Id \}\}: Volumes=\{\{ .Mounts \}\}\textquotesingle{} \textbar{} grep docker.sock & \\
\hline
\multirow{2}{=}{6} & Tránh Image sprawl & \begin{minipage}[t]{\linewidth}\raggedright
docker images -\/-quiet \textbar{} xargs docker inspect -\/-format \textquotesingle\{\{ .Id \}\}: Image=\{\{ .Config.Image \}\}\textquotesingle{}\\
docker images\strut
\end{minipage} & Rà soát toàn bộ các images hiện có trên máy chủ để phát hiện và gỡ bỏ các image không sử dụng. \\
\cline{2-4}
& Tránh container sprawl & \begin{minipage}[t]{\linewidth}\raggedright
docker info -\/-format \textquotesingle\{\{ .Containers \}\}\textquotesingle{}\\
docker info -\/-format \textquotesingle\{\{ .ContainersStopped \}\}\textquotesingle{}\\
docker info -\/-format \textquotesingle\{\{ .ContainersRunning \}\}\textquotesingle{}\strut
\end{minipage} & Rà soát tất cả các container đang chạy và đã dừng để loại bỏ các container dư thừa. \\
\hline
\end{longtable}
}

\noindent\textbf{Chi tiết quy trình và lệnh kiểm toán thủ công (Manual Audit) cho Section 1 và Section 6:}

Dưới đây là chi tiết hướng dẫn và các câu lệnh Linux CLI thực tế để quản trị viên hệ thống chạy thủ công nhằm xác minh tính tuân thủ các quy tắc bảo mật của Section 1 và Section 6:

\noindent\textbf{Section 1 -- Host Configuration (Manual Audit)}

\begin{enumerate}
    \item \textbf{1.1.1 -- Tạo phân vùng riêng cho các container:}
    Xác định Docker root directory và kiểm tra xem có được mount riêng hay không:
{\footnotesize
\begin{verbatim}
# Bước 1: Xác định thư mục root của Docker
docker info -f '{{.DockerRootDir}}'
# Ví dụ output: /var/lib/docker

# Bước 2: Kiểm tra xem thư mục này có phải mount point riêng
mountpoint -q -- /var/lib/docker
echo $?
# Nếu in ra 0: PASS. Nếu in ra 1: FAIL.
\end{verbatim}
}

    \item \textbf{1.1.2 -- Chỉ cho phép các người dùng tin cậy kiểm soát Docker Daemon:}
    Xem danh sách các user nằm trong nhóm \texttt{docker} để thẩm định thủ công:
{\footnotesize
\begin{verbatim}
getent group docker
\end{verbatim}
}

    \item \textbf{1.1.3 -- Thiết lập kiểm toán (Auditing) cho Docker daemon:}
    Kiểm tra dịch vụ \texttt{auditd} và rule của dockerd:
{\footnotesize
\begin{verbatim}
# Kiểm tra auditd đã được cài đặt
apt list --installed | grep -w auditd

# Kiểm tra audit rule cho dockerd binary
auditctl -l | grep /usr/bin/dockerd
# Output mong đợi: -w /usr/bin/dockerd -k docker
\end{verbatim}
}

    \item \textbf{1.1.4 đến 1.1.18 -- Thiết lập kiểm toán cho các tệp tin và thư mục Docker cấu hình:}
    Chạy các lệnh tương ứng để rà soát sự hiện diện của rules trong auditd:
{\footnotesize
\begin{verbatim}
# 1.1.4 - Kiểm toán thư mục /run/containerd
auditctl -l | grep /run/containerd

# 1.1.5 - Kiểm toán thư mục /var/lib/docker
auditctl -l | grep /var/lib/docker

# 1.1.6 - Kiểm toán thư mục /etc/docker
auditctl -l | grep /etc/docker

# 1.1.7 - Kiểm toán tệp tin service của docker
SERVICE_PATH=$(systemctl show -p FragmentPath docker.service \
  | cut -d= -f2)
auditctl -l | grep "$SERVICE_PATH"

# 1.1.8 - Kiểm toán tệp tin containerd.sock socket
auditctl -l | grep /run/containerd/containerd.sock

# 1.1.9 - Kiểm toán tệp tin docker.socket socket
SOCKET_PATH=$(systemctl show -p FragmentPath docker.socket \
  | cut -d= -f2)
# Đọc ListenStream trong socket unit file để lấy path thực (ví dụ /run/docker.sock)
grep ListenStream "$SOCKET_PATH"
auditctl -l | grep /run/docker.sock

# 1.1.10 - Kiểm toán tệp tin /etc/default/docker
auditctl -l | grep /etc/default/docker

# 1.1.11 - Kiểm toán tệp tin /etc/docker/daemon.json
auditctl -l | grep /etc/docker/daemon.json

# 1.1.12 - Kiểm toán tệp tin /etc/containerd/config.toml
auditctl -l | grep /etc/containerd/config.toml

# 1.1.13 - Kiểm toán tệp tin /etc/sysconfig/docker (nếu có trên RHEL/CentOS)
auditctl -l | grep /etc/sysconfig/docker

# 1.1.14 - Kiểm toán tệp tin containerd binary
auditctl -l | grep /usr/bin/containerd

# 1.1.15 - Kiểm toán tệp tin containerd-shim binary (legacy)
auditctl -l | grep /usr/bin/containerd-shim

# 1.1.16 - Kiểm toán tệp tin containerd-shim-runc-v1 binary
auditctl -l | grep /usr/bin/containerd-shim-runc-v1

# 1.1.17 - Kiểm toán tệp tin containerd-shim-runc-v2 binary
auditctl -l | grep /usr/bin/containerd-shim-runc-v2

# 1.1.18 - Kiểm toán tệp tin runc binary
auditctl -l | grep /usr/bin/runc
\end{verbatim}
}

    \item \textbf{1.2.1 -- Đảm bảo máy chủ (Host) đã được làm cứng (Hardened):}
    Chạy công cụ đánh giá bảo mật hệ thống độc lập (Lynis):
{\footnotesize
\begin{verbatim}
sudo lynis audit system
# Kiểm tra chỉ số Hardening Index trong log
grep "Hardening index" /var/log/lynis-report.dat
\end{verbatim}
}

    \item \textbf{1.2.2 -- Đảm bảo phiên bản Docker được cập nhật mới nhất:}
    Kiểm tra phiên bản hiện hành và các bản cập nhật đang chờ:
{\footnotesize
\begin{verbatim}
docker version --format '{{.Server.Version}}'
sudo apt update
apt list --upgradeable | grep -E '^(docker-ce|docker-ce-cli|containerd.io)/'
\end{verbatim}
}
\end{enumerate}

\noindent\textbf{Section 6 -- Docker Security Operations (Manual Audit)}

\begin{enumerate}
    \item \textbf{6.1 -- Tránh tích tụ quá nhiều Docker Images dư thừa (Image sprawl):}
    Đếm và đối chứng số lượng image:
{\footnotesize
\begin{verbatim}
# Số lượng images đang thực sự được sử dụng bởi các container
docker ps -qa | xargs -r docker inspect --format "{{.Image}}" \
  | sort -u | wc -l

# Tổng số images hiện có trên máy chủ
docker image list -a -q | wc -l

# Liệt kê chi tiết để rà soát
docker image list --format \
  'table {{.Repository}}:{{.Tag}}\t{{.ID}}\t{{.Size}}'
\end{verbatim}
}

    \item \textbf{6.2 -- Tránh tích tụ quá nhiều Docker Containers đã dừng (Container sprawl):}
    Rà soát các container dư thừa:
{\scriptsize
\begin{verbatim}
# Đọc thống kê containers từ hệ thống
docker info --format \
  'Containers: {{.Containers}},
   Running: {{.ContainersRunning}},
   Stopped: {{.ContainersStopped}}'

# Liệt kê tất cả container kèm theo trạng thái hoạt động của chúng
docker ps -a --format \
  'table {{.ID}}\t{{.Image}}\t{{.Status}}\t{{.Names}}'
\end{verbatim}
}
\end{enumerate}
